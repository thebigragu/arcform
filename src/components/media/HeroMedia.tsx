"use client";

/**
 * Deprecated wrapper — Gate 1 live path uses MediaView + @/media-engine.
 * Kept so accidental imports do not pull the legacy WebCodecs engine.
 */
export { MediaView as HeroMedia } from "@/components/media/MediaView";
